#!/bin/bash
#[[ "$(pgrep -x $(basename $0))" != "$$" ]] && exit 1
while [ 1 ]; do
	feh --bg-fill --randomize --recursive $1 &
	sleep $2
    if [ $(pidof -x pic| wc -w) -gt 2 ]; then
        exit
    fi
done
