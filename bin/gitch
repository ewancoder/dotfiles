#!/bin/bash
for repo in ~/.dotfiles ~/eal ~/btp /etc/.dotfiles ~/archlive/releng; do
    cd $repo
    if ! [ "$(git status | grep -i "changes\|ahead\|untracked")" == "" ]; then
        echo $repo
    fi
done
